#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"
#include "core.h"

TEST_CASE("testing the print_version function"){
    // CHECK(ECSENGINE_VERSION_MAJOR == 1);
    // CHECK(ECSENGINE_VERSION_MINOR == 0);
    CHECK(1);
}
